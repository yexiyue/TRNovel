# TRNovel 按键自定义配置示例
# 放到 ~/.novel/keybindings.toml 后生效;只写想改的条目即可,未写的保持默认。
# 键位语法(crokey,分隔符是 `-`):j、ctrl-d、shift-g(或直接写大写 G)、
# pagedown、home、f1、space、hyphen(- 键)...
# 一个操作可绑多个键(列表)或单个键(字符串);写 [] 表示解绑该操作。
# 配置有误不会影响使用:出错的条目回退默认键位,启动时弹窗提示。

