---
title: TRNovel
description: 终端里的现代小说阅读器，本地与网络小说双修，智能识别分卷目录，AI 一键生成书源，支持 TTS 听书，一个二进制开箱即用。
template: splash
hero:
  tagline: 在终端里优雅地读小说
---

import { Tabs, TabItem, LinkCard, CardGrid, Aside } from '@astrojs/starlight/components';
import Bento from '../../components/landing/Bento.astro';
import Gallery from '../../components/landing/Gallery.astro';

## 为读小说，准备好了一切

<Bento />

## 让 AI 帮你做书源

<div class="tn-split">

<div>

别人做书源要手写一堆规则，TRNovel 把「逆向、校验、导入」交给 AI 串成闭环：给一个小说站
URL，Agent 自动探站并逆向出选择器，`trn doctor` 逐项体检到全绿，导入后立刻能读。

书源是结构化 JSON，对 AI 友好，schema 由 Rust 类型自动生成。详见
[书源参考](/TRNovel/book-source/intro)与[反爬与浏览器辅助](/TRNovel/reference/anti-scraping)。

</div>

<div>

```bash title="从 URL 到能读,三条命令"
# 给 Agent 装上 skill,然后把站点 URL 交给它
npx skills add https://github.com/yexiyue/TRNovel/tree/main/skills/booksource-generator

# 全流程体检生成的书源,逐项检查到全绿
trn doctor example.v2.json

# 导入后即可在网络小说模式里阅读
trn import example.v2.json && trn -n
```

</div>

</div>

## 看它跑起来

<Gallery />

## 30 秒上手

<Tabs syncKey="install">
	<TabItem label="macOS / Linux">
		```bash
		curl --proto '=https' --tlsv1.2 -LsSf https://github.com/yexiyue/TRNovel/releases/latest/download/trnovel-installer.sh | sh
		```
	</TabItem>
	<TabItem label="Windows">
		```powershell
		powershell -ExecutionPolicy Bypass -c "irm https://github.com/yexiyue/TRNovel/releases/latest/download/trnovel-installer.ps1 | iex"
		```
	</TabItem>
	<TabItem label="Homebrew">
		```bash
		brew install yexiyue/tap/trnovel
		```
	</TabItem>
	<TabItem label="cargo">
		```bash
		cargo install trnovel
		```
	</TabItem>
	<TabItem label="npm">
		```bash
		npm i -g @trnovel/trnovel
		```
	</TabItem>
</Tabs>

装好后即可开读：

```bash
trn            # 打开主页
trn -q         # 续读上一次的位置
trn -n         # 网络小说
trn -l ./书库   # 读某个目录下的本地小说
```

<Aside type="tip">还有 `trn doctor <书源.json>` 体检书源、`trn import <书源.json>` 一键导入。完整命令见 [CLI 参考](/TRNovel/reference/cli)。</Aside>

## 继续深入

<CardGrid>
	<LinkCard title="使用指南" description="从安装到阅读、网络小说、听书、主题，逐步上手。" href="/TRNovel/guides/intro" />
	<LinkCard title="书源参考" description="书源结构、规则语法，以及如何制作一个书源。" href="/TRNovel/book-source/intro" />
	<LinkCard title="反爬与浏览器辅助" description="Cloudflare 挑战的工作方式与降级策略。" href="/TRNovel/reference/anti-scraping" />
	<LinkCard title="CLI 命令参考" description="trn 的全部子命令与参数。" href="/TRNovel/reference/cli" />
</CardGrid>
